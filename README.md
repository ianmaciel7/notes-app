# KnowledgeOS

KnowledgeOS is a private Next.js study space for reviewing cards and building
long-term memory. It uses Firebase Google authentication, localized messages,
and shared object icon and split-button components.

## Development

```bash
pnpm install
pnpm dev
```

Run checks with `pnpm check` or the test suite with `pnpm test`.
