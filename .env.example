# Copy to .env and fill only the values needed for your local workflow.

# Context7 runs in free mode; no API key is configured.

# Stitch MCP auth. Use STITCH_API_KEY only if the Stitch workflow needs it.
STITCH_API_KEY=
STITCH_USE_SYSTEM_GCLOUD=false
