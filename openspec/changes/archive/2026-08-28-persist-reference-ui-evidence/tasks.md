## 1. Agent Workflow Contract

- [x] 1.1 Update the workspace UI parity rule with reuse-first discovery, bundle identity, selective refresh, stable storage, and sanitization requirements; verify the rule names image, HTML/DOM, CSS, and JavaScript evidence.
- [x] 1.2 Update the workspace UI parity skill and add its reusable evidence-bundle reference; verify the skill routes agents to existing evidence before browser recapture.

## 2. Practical Documentation

- [x] 2.1 Add a repository reference-evidence guide and bundle manifest example; verify it defines stable paths, required metadata, artifact types, freshness, and redaction boundaries.
- [x] 2.2 Synchronize affected agent and contributor entry points; verify tracked practical docs do not retain conflicting capture guidance.

## 3. Verification

- [x] 3.1 Validate the updated skill and run strict OpenSpec validation for `persist-reference-ui-evidence`.
- [x] 3.2 Review the scoped diff and confirm unrelated workspace changes and legacy evidence artifacts remain untouched.
