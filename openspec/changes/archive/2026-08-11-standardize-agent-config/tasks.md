## Implementation

- [x] Audit `.agent/`, `.codex/`, `.gemini/`, `.agents/`, `AGENTS.md`, and `GEMINI.md`.
- [x] Migrate any portable legacy workflow or skill content needed in `.agents/`.
- [x] Update `AGENTS.md` and documentation/spec references for the vendor-neutral canonical structure.
- [x] Update `.gitignore` to ignore `.agent/`, `.codex/`, and `.gemini/` while keeping `AGENTS.md` and `.agents/` tracked.
- [x] Delete evaluated legacy vendor-specific directories and redundant vendor-specific files.
- [x] Verify OpenSpec, references, retained skills, ignored paths, and final tree state.
