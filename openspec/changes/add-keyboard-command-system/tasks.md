## 1. Isolation, Baseline, and Evidence

- [x] 1.1 Start apply work in an isolated `codex/add-keyboard-command-system` worktree from the intended `dev` base, carry only this change's OpenSpec artifacts into it, and verify `git status --short`, branch identity, and tracked diff exclude the unrelated dirty test and local artifact changes.
- [x] 1.2 Read the installed Next.js guides relevant to any workspace-boundary or routing files selected during apply and record the applicable current APIs in the implementation notes; verify no implementation relies on training-era routing assumptions.
- [x] 1.3 Search `docs/references/`, `artifacts/reference-evidence/`, and `artifacts/capacities-reference/` for palette, slash, object-reference, and block-reference states; verify reused evidence is indexed with source, timestamp, viewport, semantic state, and interaction coverage before any live recapture.
- [x] 1.4 Complete a safe reference/local action matrix for palette and editor triggers covering idle, hover/exit, focus, ArrowUp/ArrowDown, Enter, Escape, outside dismissal, post-action, persistence, unavailable, IME, reduced-motion, responsive, and console states; verify every state has evidence or an explicit `not tested`/`mutation prohibited` verdict.
- [x] 1.5 Add or refresh only missing sanitized evidence under `artifacts/reference-evidence/` with a valid manifest, DOM/accessibility, geometry/style, behavior, transition, redaction, and limitation records; verify `docs/references/` links the bundle without persisting cookies, storage, credentials, unrelated personal data, or complete third-party exports.

## 2. Command and Shortcut Foundation

- [x] 2.1 Add focused failing tests for platform-neutral chord parsing/formatting, `Mod+K`/`Mod+P`, modifier ordering, Windows/Linux versus macOS labels, and invalid chord rejection; run the focused node test and verify each new assertion fails for the missing command module.
- [x] 2.2 Implement the minimal pure shortcut metadata and formatting contract, then run the focused test and verify all red cases from 2.1 pass without adding a hotkey dependency.
- [x] 2.3 Add focused failing tests for command identity, localized metadata projection, availability, stable ordering, runtime Structure creation projection, and same-action execution from two callers; verify failures demonstrate the absence of the central registry rather than fixture or import errors.
- [x] 2.4 Implement the minimal declarative command registry and workspace runtime adapter over existing navigation, creation, settings, calendar/today, sidebar/panel, and focus owners; verify the new registry tests plus existing object lifecycle and sidebar source-contract tests pass and unsupported actions are omitted truthfully.
- [x] 2.5 Add focused failing tests for context priority, editable targets, IME composition, accepted-command `preventDefault`, modal/specialized/editor/page/global arbitration, and selected-text `Mod+K` link precedence; verify the tests fail before the router exists.
- [x] 2.6 Implement one workspace-boundary shortcut router and migrate duplicated palette-opening shortcut metadata/listeners to registry consumption; verify router tests pass, no new feature-level `document.addEventListener("keydown")` is introduced, and current visible controls still call their existing canonical owners.

## 3. Shared Search and Global Palette

- [x] 3.1 Add focused failing query-engine tests for plain normalization, `^` leading-content priority, fully quoted exact phrases, exact/title-leading/strong/partial/approximate ordering, alias deduplication, stable tie-breaking, object-only/block-only modes, owning-object block context, and committed-content index updates; verify expected failures are caused by missing query intent and ranking behavior.
- [x] 3.2 Extend the existing workspace search index and result types with the minimal parsed query intent and deterministic ranking needed by 3.1; verify focused query-engine, linking, snapshot migration, and representative large-fixture timing tests pass without a second index or per-keystroke persistence.
- [x] 3.3 Add failing component tests for one `CommandDialog` palette opened by `Mod+K` and `Mod+P`, local query control, grouped available commands/object/block results, deterministic active identity, ArrowUp/ArrowDown/Enter/Escape, empty/loading/error state, focus restoration, and platform shortcut labels; verify the unimplemented palette produces the expected red results.
- [x] 3.4 Implement the shadcn-style workspace command palette by composing existing `src/components/ui/command.tsx`, registry projections, deferred search results, runtime object icons, and next-intl catalogs; verify component tests, locale completeness checks, data-slot rules, and existing shared-popup source tests pass.
- [x] 3.5 Wire the palette and router once at the workspace boundary and make eligible sidebar/search/menu consumers invoke the same open state and command actions; verify `Mod+K`, `Mod+P`, pointer invocation, navigation, runtime object creation, and result selection each produce one canonical post-action state.

## 4. Shared Editor Suggestion Infrastructure

- [x] 4.1 Add focused failing tests for trigger-neutral caret rectangle handling, document-position fallback, 8px viewport containment, flip behavior, active identity, pointer/keyboard selection, Escape/outside dismissal, focus recovery, reduced motion, and no `(0,0)` flash; verify the tests fail against the current slash-only renderer.
- [x] 4.2 Extract the minimal shared Tiptap Suggestion controller and adapt `/` as its first consumer without changing the slash catalog or command transactions; verify the new controller tests and all existing slash ordering, alias, geometry, selection, localization, undo/redo, and persistence tests pass.
- [x] 4.3 Add failing trigger-arbitration tests for valid text boundaries, unsupported nodes, overlapping suggestions, deletion of trigger ranges, IME composition, and leakage into global shortcuts; verify failures isolate missing trigger ownership rather than existing slash behavior.
- [x] 4.4 Implement shared trigger ownership and composition guards, then verify the arbitration suite passes and typing ordinary punctuation in unsupported contexts remains unchanged.

## 5. Object and Block Reference Triggers

- [x] 5.1 Add failing domain/editor tests for `@` and `[[` opening the same eligible object lookup, title/alias normalization, same-target deduplication, duplicate visible-label disambiguation with runtime Structure context, exact trigger-range replacement, and Escape cancellation; verify both triggers fail before their adapters exist.
- [x] 5.2 Implement the shared object-result provider plus `@` and `[[` trigger adapters using the existing structured object-link mark and canonical eligibility checks; verify selection creates one rename-safe reference transaction, restores focus, participates in undo/redo, and updates backlinks, counts, graph, search, and persistence exactly once.
- [x] 5.3 Add failing domain/editor tests for `((` block lookup, duplicate block text, owning-object context, stable object/block identity, target invalidation, reordering/text-change survival, trigger-range replacement, and Escape cancellation; verify the missing block adapter is the cause of each failure.
- [x] 5.4 Implement the block-result provider and `((` adapter using the existing stable block-link mark and commit-time identity validation; verify one selection creates one correct block reference and rejected or cancelled selection leaves document, selection, link projections, and persistence unchanged.
- [x] 5.5 Run the complete slash/object/block suggestion suite in sequence and verify only one surface owns a range, every accepted action is one transaction, IME and rapid trigger switching do not produce partial marks, and all existing object-page mention/editor-utility tests remain green.

## 6. Browser Acceptance, Performance, and OpenSpec Completion

- [x] 6.1 Run matched browser checks on the production workspace route for `Mod+K`, `Mod+P`, contextual selected-text `Mod+K`, palette search/commands/navigation/creation, `/`, `@`, `[[`, and `((`; verify the action matrix records roles/names, rectangles, computed styles, hover/focus/click/keyboard transitions, focus restoration, post-action projections, persistence, responsive containment, reduced motion, and console state.
- [x] 6.2 Measure palette-open and query interaction against representative large local fixtures; verify input remains synchronous, derived work may lag without dropping characters, stale work cannot replace current results, and no workspace update or localStorage write occurs on each palette/editor keystroke.
- [x] 6.3 Run focused tests followed by typecheck, lint, locale validation, the repository verification command, and the relevant browser suite; verify zero implementation console/hydration errors, duplicate persistence writes, flaky identity ordering, or unrelated file changes remain.
- [x] 6.4 Reconcile overlapping `/` and `@` deltas with `align-object-page-complete-parity`, update stable design/testing/reference documentation where behavior changed, run `openspec validate add-keyboard-command-system --strict`, and verify proposal, four delta specs, design, tasks, evidence index, and practical documentation remain coherent.
- [x] 6.5 Use the OpenSpec sync workflow after implementation is stable, rerun `openspec validate --specs`, archive the completed change, and verify archived artifact lookup plus the canonical specs preserve every accepted command, search, shortcut, and editor-reference requirement.
