## 1. App header component

- [x] 1.1 Add a reusable `AppHeader` component under `src/components` using existing shadcn `Button` primitives and Lucide icons.
- [x] 1.2 Add back, forward, create, and focus actions with typed callbacks and independent disabled states for history actions.
- [x] 1.3 Keep the center region composable through `children` and expose stable `data-slot` hooks.

## 2. Integration

- [x] 2.1 Integrate the new header into the current desktop app shell without changing side-panel or mobile shell behavior.

## 3. Verification

- [x] 3.1 Keep implementation aligned with existing shadcn-style project conventions and native component sizing.
- [ ] 3.2 Run the full repository verification workflow in CI or a development checkout.
