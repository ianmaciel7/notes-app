## Apply Tasks

- [ ] Update `.openspec.yaml` in `restore-legacy-agents-assets` to include `skip_specs: true`.
- [ ] Restore `.agents/mcp-servers.json` from `origin/old`.
- [ ] Restore `.agents/skills` from `origin/old`.
- [ ] Validate `.agents/mcp-servers.json` has required top-level fields and includes expected MCP server entries.
- [ ] Update `README.md` and entrypoint docs (`AGENTS.md`, `CLAUDE.md`, `GEMINI.md`) if needed to document the restored manifest and installation steps.
