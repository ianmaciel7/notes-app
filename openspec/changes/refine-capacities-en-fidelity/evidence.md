# Capacities `/en` Fidelity Evidence

## Reference corpus availability

| Requested reference | Current result |
| --- | --- |
| `capacities-wacz-completeness-audit.json` | Supplied in `Downloads`; confirms 826 WARC records and 405 recoverable responses through 309 deduplicated response resources |
| `capacities-wacz-complete-source.jsonl` | Supplied in `Downloads`; contains the recoverable resource corpus used by the visual extractor |
| `my-archiving-session (1).wacz` | Supplied in `Downloads`; hashed and recorded as a source archive |
| `my-archiving-session.wacz` | Supplied in `Downloads`; hashed and recorded as a source archive |

The corpus is complete for the captured HTML/CSS/JavaScript/JSON UI resources, although the audit explicitly does not claim a byte-identical ZIP reconstruction and records four unavailable image payloads plus redacted request/authentication data. The authenticated live target remains the primary state/geometry evidence; extracted WACZ tokens are the primary captured source evidence for colors, radii, borders, and shadows. Historical branch measurements remain secondary and must be rechecked visually before reuse.

`artifacts/capacities-reference/capacities-wacz-visual-contract.json` is the sanitized, single-file consultation contract generated by `scripts/extract-capacities-wacz-visual-contract.mjs`. It records source hashes, eight Capacities CSS resources, 280 custom-property observations, 144 targeted rules, the resolved light theme, and the object-type `background=75`, `border=300`, `text=650` tone mapping without retaining cookies, query strings, authentication state, or proprietary JavaScript bodies.

## Historical donor availability

| Donor | Availability | Intended use |
| --- | --- | --- |
| `old` | `origin/old` | Rich menus, tooltips, keyboard hints, reduced motion |
| `old-2` | Local and `origin/old-2` | Deterministic fixture, typography/tokens, contextual sizing, object workspace |
| `old-3` | `origin/old-3` | Complete three-pane composition and context controls |
| `feat/app-sidebar` | Separate linked worktree and remote | Narrow side-panel close behavior only |

## Baseline mismatch ledger

| Category | Reference behavior/value | Current behavior/value | Evidence source | Owning local file | Historical donor | Proposed fix |
| --- | --- | --- | --- | --- | --- | --- |
| 1. App shell geometry | 288px sidebar, 46px rails, about 10px panel gaps, main wider than bounded context | Sidebar/rails match; context begins at 45% and is too wide | Live screenshot/computed boxes; Graphify | `app-shell.tsx` | `old-2`, `old-3` | Retain resizable API and tune defaults/bounds/margins |
| 2. Sidebar | Persistent switcher, compact 32/29px rows, target object selected | Structure/density close; Calendar selected and menu metrics differ | Live screenshot/DOM; historical audit | Sidebar composition files | `old`, `old-2`, `old-3` | Align route selection and measured consumer styling |
| 3. Header/history | Back/forward, one active target tab, add action | Five unrelated demo tabs | Live screenshot/DOM | `app-header-demo.tsx` | `old-3` | Replace only route fixture state |
| 4. Main tabs | One compact 32px target tab | Cloud/Azure/Cursos/Ideias fixture | Live screenshot; active design | `app-header-demo.tsx` | `old-3` | Use accepted target state; preserve overflow API |
| 5. Side-panel tabs | Active Explore tab plus add/toggle/menu | Extra graph-view tab precedes Explore | Live screenshot/DOM | Side header and fixture | `old`, `old-3`, feature donor | Align fixture; port confirmed close behavior only |
| 6. Main content | Object title/icon/New, view tools, count/sort tools, centered empty state | Empty Card | Live authenticated target | New route-owned component | `old-2`, `old-3` | Add typed presentation-only fixture |
| 7. Context content | Centered explore cards and relevant-content row | Empty Card | Live authenticated target | New route-owned component | `old-2`, `old-3` | Add typed presentation-only fixture |
| 8. Typography | `Inter, ui-sans-serif, system-ui` | `Geist, Geist Fallback` | Live computed styles | Layout/global font boundary | `old-2`, `old-3` | Follow installed Next 16 font guidance |
| 9. Color/tokens | Warm off-white base, white fronts, subtle warm borders | Similar header; generic Nova tokens and local hex | Live styles/screenshot | Globals and composition owners | `old-2` | Promote confirmed shared values only |
| 10. Borders/radii | 12px surfaces with subtle border | 14px Cards and different margins | Live computed styles | `app-shell.tsx` | `old-3` | Match surface radius/border locally |
| 11. Icons | Small neutral line icons with colored object chips | Lucide approximations and wrong fixture icons | Live screenshot | Icon modules and fixture | `old-2`, `old-3` | Keep Lucide; align size/tone/chips |
| 12. Hover/focus/active | Affordances reveal on hover/focus; subtle active object row | Behaviors exist; wrong route selection/default metrics | Live DOM; current contracts | Sidebar/header consumers | `old`, `old-2` | Preserve semantics and tune confirmed states |
| 13. Menus/popovers/tooltips | Compact menus, 12px radius, close offsets, rich hints | Functional Base UI with generic widths/offsets | Historical audit; current source | Consumers | `old`, `old-2` | Port narrow visible dimensions/hints |
| 14. Motion | Smooth resize/collapse, no flash, reduced motion | Opacity/flex transition needs regression testing | Donor behavior/current design | `app-shell.tsx` | `old`, `old-2` | Preserve primitives and tune ownership |
| 15. Resize/collapse | Bounded contextual width and usable triggers | Broad 10-90% range, no width persistence | Source/design; donor measurements | `app-shell.tsx` | `old-2`, `old-3` | Narrow defaults/bounds if live confirms |
| 16. Responsive/mobile | Supported breakpoint remains usable with sidebar/context access | Sheets exist below 768px but fixture content absent | Current design/browser checks | Shell and fidelity component | `old-3` | Keep Sheets and prevent overflow |

## Baseline visual assets

- `capacities-reference-1440x900.png`: accepted authenticated state captured at its actual 1294x912 viewport.
- `notes-app-before-1440x900.png`: initial local state with empty surfaces and unrelated tabs.
- Transient Capacities "view not ready" reload states are not accepted as the canonical target.

## Header-tab regression correction

- Reference at 1294x912: one active `Notas atômicas` tab measured about 149px wide, followed by the add-tab control; hovering the control exposed `Criar nova aba`.
- Broken local state: repeated add actions accumulated five 60px `Nova página` tabs while the main surface continued to show `Notas atômicas`, so navigation state and content disagreed.
- Corrected local behavior: the add action creates one reusable `Nova aba` draft and opens a search dialog; repeated add actions do not accumulate drafts, Escape cancels the draft, and selecting an item replaces the draft with a deduplicated tab whose label also drives the main heading.
- At 1294x912, three selected tabs remained usable at the 96px minimum and the tab-list affordance remained available. The document reported `scrollWidth === clientWidth === 1294`.
- At 390x844, the desktop shell was hidden, the mobile shell remained usable, and the document reported `scrollWidth === clientWidth === 390` with no fresh console errors.
- The browser backend returned 1280x720 when asked for 1024x768, so exact 1024px desktop fidelity is not claimed and task 4.3 remains open.

## Latest authenticated citation target

- URL state: `Sem título · Capacities`, with `Citações` selected in the sidebar and `Sem título` active across five visible main tabs.
- At 1294x912, the body uses `Inter, ui-sans-serif, system-ui`, `oklch(0.9856 0.0016 67)` background, `oklch(0.2191 0.0058 285.84)` foreground, and hidden document overflow.
- The main surface is x=298, y=46, w=533.3, h=856; the contextual surface is x=841.3, y=46, w=442.7, h=856. Both are white, have a 1px `oklch(0.9163 0.0017 67.07)` border, and a 12px radius.
- The citation editor card is x=311, y=71, w=507.3, h=301.8 with a 16px radius. Its title input is 30px/700 with 33px line-height.
- The contextual action grid is 330.5px wide, uses three columns and two rows, 8px gaps, and six 90px-tall cards with 8px radii and 16px padding.
- The `Novo` palette is x=15, y=78, w=352, h=361 with a 12px radius, a 32px muted search control, and 13 visible/scrollable object choices. The prior local palette was x=8, y=82, h=399.4 with an 8px radius and only nine choices.
- The space switcher is x=152, y=45, w=300.5, h=476 with a 12px radius and `Criar espaço` footer. The prior local switcher was x=148, y=13, w=288, h=299 with an 8px radius and English labels.
- `artifacts/capacities-reference/visual-contract-2026-08-22.json` stores sanitized asset URLs, computed tokens, geometry, and visible labels. It intentionally excludes cookies, authentication state, full HTML, and proprietary CSS/JavaScript source.
- `artifacts/capacities-reference/capacities-wacz-visual-contract.json` stores the reproducible captured-source color/radius/shadow contract and supersedes guessed icon-tone hex values.
