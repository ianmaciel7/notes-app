The repository maintains 10 historical checkouts in .worktrees/old-* containing earlier, focused prototypes. old-9 contains the complete domain, auth, and tenancy baseline with 1:1 unit test companions. old-4 holds the block editor and micro-contract suites. old-2 holds responsive ARIA drawer navigation. old-5 holds sync queues and interaction telemetry. old-8 holds card study UX. Code in .worktrees/ is strictly read-only prior art and must never be imported into runtime code. When scouting worktrees, use subagents and enforce the minimal test per code pattern.