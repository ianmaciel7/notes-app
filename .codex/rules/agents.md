# Codex project rules

The canonical project rules are in the repository root at `AGENTS.md`.

Codex agents must read and follow that file before working. Keep this Codex
rule entry aligned with `.agents/rules/agents.md` if platform-specific
discovery requires a local copy; do not create conflicting instructions here.
