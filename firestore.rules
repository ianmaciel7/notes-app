rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Application reads and writes use authenticated server actions.
    match /{document=**} { allow read, write: if false; }
  }
}
