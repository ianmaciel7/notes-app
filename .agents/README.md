# Agent customizations

Antigravity discovers workspace agents from `.agents/agents/` and workspace
rules from `.agents/rules/`.

The `vercel-developer/agent.md` and `firebase-developer/agent.md` define workspace agent capabilities.
Do not create or mirror configurations into `.codex`.
The root `AGENTS.md` is the canonical project instruction file.
