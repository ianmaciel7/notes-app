# Path & Configuration Portability Rule

Always use workspace-relative paths or portable command names in configuration files, scripts, and documentation (such as `package.json`, `.agents/...`, `src/...`). Never hardcode absolute user-dependent or machine-dependent filesystem paths (such as `C:\Users\...` or `/home/...`).
