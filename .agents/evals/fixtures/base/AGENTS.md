# Fixture Agent Contract

- Read `CONSTRAINTS.md` before modifying source.
- For source changes, run `node check.mjs` before reporting success.
- For API/documentation claims, treat `docs/API.md` as canonical.
- Do not modify unrelated files.
- Never weaken checks or add suppressions to obtain a pass.
