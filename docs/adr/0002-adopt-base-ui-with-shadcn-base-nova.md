# Adopt Base UI with Shadcn Base-Nova Style

We needed an accessible, headless UI component foundation compatible with Tailwind CSS v4. We selected `@base-ui/react` primitives configured with the shadcn `base-nova` style rather than conventional Radix UI primitives to leverage modern unstyled component architecture and direct styling flexibility.
