# Objects inside location matrix

Viewport: `1319x912` for the user-reported local state. The authenticated reference was inspected read-only and contained different canonical data.

| Action | Expected reference state | Observed localhost before change | Verdict | Evidence |
| --- | --- | --- | --- | --- |
| Open an object in the main page | `Objects inside` is not rendered as a main-page relationship section. | The embedded object was rendered and then duplicated under a main-page `Objects inside` heading. | Main-page location mismatch | `reference-readonly-observation.json` |
| Open the contextual side-panel menu | `Objects inside` remains a dedicated contextual destination. | The local contextual destination exists and is covered by the parity browser suite. | Contract retained | Official navigation documentation; `tests/e2e/workspace-parity.spec.ts` |
| Load a local page containing an embed | The embed stays available in the page while contextual discovery remains separate. | After the change, the embedded editor remains visible and the duplicate main-page heading is absent. | Pass | Focused Playwright regression test |
| Compare the editable title at the matched viewport | A transparent, one-row `TEXTAREA` occupies `728x39` at `x=438.5`, with 30/33 typography. It has no visible form box. | Localhost now uses the same semantic element, geometry, typography, transparent background, and resize behavior. | Pass | `matched-dom-css-behavior.json`; paired screenshots |
| Compare centered content and related rows | A 10px scrollbar reserve keeps the 800px column at `x=398.5`; related headings and rows use 8px radii, and row titles use 16/24 typography. | Localhost now matches these measurements. | Pass | `matched-dom-css-behavior.json`; focused Playwright regression test |
| Toggle a related-row disclosure at `604x910` | The 33px row grows to 76px through a full-width 43px body preview; a 2px guide sits 10px from the left, the editable body uses 17/28 typography with a 28px inset, and no second title appears. A second click restores the collapsed state. | Localhost now matches the same geometry, editable-body semantics, title placement, and restore behavior. | Pass | Paired collapsed/expanded screenshots; `matched-dom-css-behavior.json`; focused Playwright regression test |
| Hover the disclosure | Only the disclosure affordance receives a hover surface; the outer related row remains transparent. | Localhost now keeps the outer row transparent while revealing the disclosure affordance and row actions. | Pass | Paired screenshots; focused Playwright regression test |
| Hover anywhere in Related content at `604x910` | A compact 28px `SlidersHorizontal` action appears at the section's top-right corner; the wide layout retains the text action. | Localhost now uses the same section-hover trigger, compact geometry, icon, and responsive text fallback. | Pass | Live computed-style inspection; paired screenshots; focused Playwright regression test |
| Render the object page at `604x910` | The 572px content column uses 40px left, 32px right, and 36px top padding, placing the title at `x=51`, width `500`. | Localhost now resolves to the same column and title geometry. | Pass | `matched-dom-css-behavior.json`; focused Playwright regression test |
| Type consecutive body blocks at `604x910` | Main body text uses 17px type on a 28px line with 20px between adjacent blocks, producing a 48px vertical step. | Localhost now uses the same type, line height, block gap, and vertical step in the shared editor contract. | Pass | Live computed-style/geometry inspection; paired main-body screenshots; focused Playwright regression test |
| Render two or more related rows | Each collapsed row remains 33px high and advances 45px vertically to the next row. | An isolated two-relation fixture now reproduces the same 45px row step. | Pass | Live reference geometry; `localhost-related-two-rows-604x910.png`; focused Playwright regression test |
| Inspect a related-row object-type badge | The Page badge is about 20.7px high with 11/14.3 typography, a 5.225px radius, tone-specific colors, and a right edge flush with the row. | Localhost now uses the same measured geometry and the existing object-type tone palette. | Pass | Live DOM/CSS geometry; isolated browser regression and screenshot |

Not tested: mutating authenticated Capacities content to add or remove an embed.
