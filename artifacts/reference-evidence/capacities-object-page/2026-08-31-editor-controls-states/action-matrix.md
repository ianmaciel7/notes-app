# Object Page Editor Control Action Matrix

Viewport: 1282 x 912. Reference and localhost use different block text, so horizontal edge geometry and interaction state are authoritative; absolute vertical positions are descriptive only.

| Control | Action | Reference state | Local state | Verdict | Evidence |
| --- | --- | --- | --- | --- | --- |
| Block controls | Idle | Handle hidden outside the active block. | Handle hidden outside the active block. | Pass | `reference-block-handle-idle.png`; `local-block-handle-idle.png` |
| Block controls | Hover block | Text begins at x=339. Plus is 18x22 at x=305; grip is 18x22 at x=321 and ends flush at x=339. Effective handle opacity is 0.5 with a 100ms linear transition. | Text begins at x=339. After correction, the 36x22 surface begins at x=303; plus is 18x22 at x=303 and grip is 18x22 at x=321, ending flush at x=339. Effective opacity is 0.5 with a 100ms linear transition. | Pass; reference plus extends 16px beyond its 18px grip wrapper while local owns one explicit 36px surface | `reference-block-handle-hover.png`; `local-block-handle-hover.png`; computed-style probe |
| Grip | Click | Opens the reference block action menu with hierarchy, list, to-do, quote, toggle, group, AI, transform, copy, duplicate, and remove families. Opening is non-mutating. | Opens an accessible `menu` with insertion, hierarchy/list/block transforms, duplicate, and delete rows. | Pass for open/close contract; catalog scope differs by supported schema | `reference-grip-click-menu.png`; `local-grip-click-menu.png`; DOM snapshot |
| Grip | Escape | Menu closes; reference focus falls back to `body`. | Menu closes and focus returns to `button[aria-label="Opcoes do bloco"]`. | Local stronger semantics | DOM/focus probe |
| Plus | Click / Shift-click | Mutation prohibited in authenticated reference; archived evidence confirms below/above insertion. | Isolated Playwright test inserts one paragraph below and one above, focuses the new block, and preserves the owning block identity. | Local pass; reference live mutation not tested | `docs/references/capacities-block-handle.md`; focused Playwright run, 5/5 combined rows passed |
| Slash | Open after whitespace | Reused current project reference: caret-anchored 440px menu with leading order Padrao, Pequeno, Cabecalho 1-4, Lista de marcadores, Lista alfabetica. | Isolated browser test opens after existing text, stays by the caret, preserves leading order, and has a clean console. | Pass | `docs/references/capacities-slash-menu.md`; focused Playwright run |
| Slash | Invalid boundary / scroll / viewport bottom | Reused reference contract: no mid-word trigger, follow live caret, 8px clamp and flip above when needed. | All four focused browser assertions passed, including mid-word rejection, scroll reposition and flip above. | Pass | `docs/references/capacities-slash-menu.md`; focused Playwright run |
| `@` objects | Title and alias lookup | Not tested live: opening the menu requires typing into authenticated content. No remote mutation was authorized. | Domain tests confirm normalized title/alias lookup, one row per object identity, runtime Structure eligibility, rename safety, backlink refresh, exact range replacement, and atomic rejection. | Reference not tested; local domain pass | `tests/editor-reference-suggestions.test.mjs`; `tests/workspace-object-links.test.mjs` |
| Customize | Idle away from header | Hidden in the reference. | Hidden and inert: opacity 0, pointer-events none, even after another header field previously had focus. | Pass | `local-customize-idle-after-hover-fix.png`; computed-style probe |
| Customize | Header hover | Reveals only on header hover/open interaction. | Reveals at opacity 1 with pointer-events auto only while the header is hovered, popup-open, or keyboard focus-visible. | Pass | `local-customize-header-hover-after-fix.png`; computed-style probe |
| Related-content row | Idle / hover | Reference rows are 36px high, borderless, and transition the background in 100ms; the row background changes from transparent to the front-hover token. | Local row is 36px high, borderless, and transitions from transparent to the muted hover token in 100ms. Independent row actions change from opacity 0 / pointer-events none to opacity 1 / pointer-events auto. | Pass | `local-related-idle.png`; `local-related-row-hover.png`; reference/local computed-style probes |
| Related-content disclosure | Click / click again | Chevron toggles an inline preview without navigating. | `aria-expanded` changes to `true`, preview renders, and the second click collapses it without changing the object route. | Pass | `local-related-expanded.png`; DOM/ARIA probe |
| Related-content title | Click | Opens the selected related object in the main surface. | Main URL changed to the selected related-object ID; returning restored the original route. | Pass | URL transition probe |
| Related-content side-panel action | Hover / click | Auxiliary row action appears on hover and opens the object contextually. | Hover reveals an accessible `Abrir no painel lateral` button; click selects the related object in the side-panel tablist. | Pass | `local-related-row-hover.png`; `local-related-side-panel-click.png`; selected-tab probe |
| Related-content options | Hover / click / Escape | Auxiliary ellipsis appears on hover and exposes row actions. | Click opens an accessible menu with main and side-panel actions; Escape closes it and restores focus to `workspace-object-related-content-options`. | Pass | `local-related-options-open.png`; DOM/focus probe |
| Related-content Show more | Idle / section hover / click | The continuation action is revealed by section hover and opens the contextual related-content surface. | Idle is opacity 0 and pointer-events none; section/row hover changes both to visible/interactive. Click is wired to the named related-content side-panel entry. | Pass | `local-related-idle.png`; `local-related-row-hover.png`; computed-style and DOM probe |
| Collections | Hover / click / Escape | Inline 25.78px transparent property trigger with pointer cursor; click focuses the `Coleções` textbox and exposes collection choices without committing. | Inline 25.80px transparent trigger; hover retains compact geometry, click opens the selector dialog, and Escape cancels without a collection update. | Pass | `local-collections-hover.png`; `local-collections-open.png`; DOM/geometry probe |

## Runtime notes

- Focused browser command: `playwright test block-editor-interactions.spec.ts slash-menu-reference.spec.ts --grep "plus and six-dot|slash menu opens after existing text|slash does not open|slash menu follows|slash menu flips"`.
- Result: 5 passed for the combined handle/slash run; the post-refactor slash-only run passed 4/4.
- The current in-app browser could inspect hover/click geometry but did not retain focus in the local `contenteditable` for injected `/` or `@`; that limitation is kept separate from the isolated browser result.
