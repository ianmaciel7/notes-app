# Empty custom-object action matrix - 2026-09-01

Viewport: `1282x912`. Mode: authenticated, initially read-only, followed by one user-authorized reversible collection-creation probe. The route, account identity, and unrelated private content are redacted.

State legend: **CONFIRMED** was measured in this refresh; **INFERRED** reuses a compatible earlier object-page observation but was not re-exercised on this exact custom object; **NOT TESTED** was intentionally left open.

| Surface/action | Reference state | Verdict | Evidence |
| --- | --- | --- | --- |
| Empty custom-object composition | Header content begins at x=424 with a 728px content measure. The visible order is type, Collections, hover-only Customize/overflow, Title, Tags, hover-only Add property, then the empty editor placeholder `Text`. No derived relationship section is rendered in this empty state. | **CONFIRMED** | DOM snapshot, bounding rectangles, and visible-text inventory. |
| Type chip idle | `Objeto` is a `101.453x25.781px` compound chip with blue text/background/border and `6.65px` radius. | **CONFIRMED** | Computed style and geometry probe. Type picker open/selection is **NOT TESTED**. |
| Collections idle/open empty | Idle trigger is approximately `97.6x25.4px`; its native input is `62x18.2px`. Opening focuses the inline textbox named `Coleções`. The `257.6x45.6px` popup has 6px padding and one initially highlighted `244x32px` row, `Novo Coleção`, with a 14px Phosphor Plus icon. Opening alone does not mutate state. | **CONFIRMED** | DOM/accessibility, focus, SVG path, geometry, and computed-style probes. |
| Collections filtering and keyboard | When matching unselected collections exist, the popup shows those collection rows and omits the create row. Selected collections disappear from the choices. When no result matches, only `Novo 'query'` appears. ArrowUp/ArrowDown retain input focus and move or retain the active row; Enter activates it. Selecting an existing collection keeps the popup and input open, removes that newly selected row, and reveals `Novo Coleção` when no unselected choice remains. Escape closes, clears the query, and leaves focus on the neutral document body. | **CONFIRMED** | Before/after visible inventory, active element, row highlight, query, Enter, and Escape probes. |
| Collections direct create and persistence | Clicking `Novo Coleção` creates and selects `Sem título` immediately without an intermediate form. A named no-match query follows the same direct-create transaction. The new header chip uses a collection icon and a hover-only trailing remove target; removing it changes membership without deleting the collection. The collection and membership survive reload and project to the sidebar. | **CONFIRMED** | Click, post-action chip/sidebar state, membership remove/reselect, and reload probes. One exact audit collection remains pending deletion because the final destructive confirmation was not accepted. |
| Customize hover | The `135.266x26px` target is inert at idle and becomes pointer-active on header hover through a `300ms linear` opacity transition. The measured hover opacity on this object was `0.5`; geometry stayed fixed. | **CONFIRMED** | Computed style and rectangle probe. |
| Customize open | Empty custom object exposes `Gerar Título`, `Preencher Todas as Propriedades`, and `Layout Amplo`, each with a leading icon. Surface is approximately `277.156x110px`, with 32px rows, 14/20px type, 12px radius, 1px semantic border, and the shared three-layer shadow. Escape closes and restores focus to the Customize trigger. | **CONFIRMED** for open/close; command activation/persistence **NOT TESTED** | DOM, SVG count, computed style, geometry, and focus probe. |
| Overflow open | Surface is approximately `269.531x475px`. Current catalog: Customize, Use Template, Pin to Sidebar, Change Type, Object Type Settings, Share, Present, Export, Import, Text Statistics, Copy, Duplicate, Delete Object. `Find in Page` and `Edit Collections` are absent in this custom-object state. | **CONFIRMED** for catalog/open state; command activation and close paths **NOT TESTED** | DOM/accessibility inventory and computed geometry. |
| Add property idle/hover | The action owns a stable 32px row between Tags and the editor. Its ancestor is opacity 0 at idle and reveals through a 300ms ease-out transition on property-region hover; the target is not a permanent visible placeholder. | **CONFIRMED** | Computed opacity, transition, and rectangle probe. |
| Add property open | The popup is `290x430px` at `(419,306)`, with a 1px semantic border, 12px radius, shared three-layer shadow, and 6px search padding. The `Buscar propriedades` input is focused in a `276x32px` muted search row. | **CONFIRMED** | DOM/accessibility, focus, geometry, and computed-style probe. |
| Add property catalog/order | 32px rows appear in this order: Text, Content, Label, Object selection, Checkbox, Date and time, Number, Description, Cover image, Icon, Created at, Last updated, Aliases. Each row owns a leading property-type icon. | **CONFIRMED** | Visible-text order, row geometry, and SVG inventory. |
| Object selection submenu | A later read-only refresh on the same authenticated route confirmed that hovering Object selection opens a second menu with a focused `Buscar tipos de objeto` field and the available runtime object types. At the browser's 1536px-wide default viewport, the surface measured `222x345.6px` with 12px radius. Reference selection remained **NOT TESTED** to avoid changing the account. The [official Object Properties documentation](https://docs.capacities.io/reference/object-properties) confirms that one linked object type is chosen and the new property defaults to multiple selection with no restrictions. Local browser test `object selection property chooses one runtime Structure before committing` verifies non-mutating cancellation and one constrained, multiple entity-property commit. | **CONFIRMED** for reference open/search geometry and official setup semantics; reference mutation **NOT TESTED**; local implementation **PASS** | Live DOM/accessibility/geometry probe, official docs and setup image, focused Playwright persistence assertion. |
| Add property keyboard/cancel | ArrowDown keeps focus in the search input and moves the highlighted 32px row from Text to Content. Escape closes without adding a property and leaves focus on the neutral document body. | **CONFIRMED** | Active-element and row-background probes before/after ArrowDown and Escape. |
| Add property semantics | Reference rows are generic pointer targets without explicit menu/option roles or sequential `tabIndex`; this is a reference accessibility weakness and must not be copied locally. | **CONFIRMED** | DOM role/tab-index probe. |
| Tags open/empty | The idle Tags trigger is present. Exact empty-popup contents, keyboard behavior, and cancellation were not exercised in this refresh. | **NOT TESTED** | Baseline DOM only. |
| Editor `+`/grip and related sections | The empty editor placeholder is present. Current exact-object handle hover, grip menu, slash, `@`, and relationship/related-content transitions were not re-exercised; use the 2026-08-31 bundles only as compatible prior evidence, not as a current custom-object parity pass. | **INFERRED / NOT TESTED** | `../2026-08-31-editor-controls-states/` and `../2026-08-31-browser-comments-hover-states/`. |

One collection was created with explicit user authorization to prove direct creation and reload persistence. Its object membership was returned to the original empty state. Final deletion was not executed because the browser required a fresh destructive confirmation; the exact pending artifact was reported to the user outside this sanitized bundle. No other create, edit, upload, share, export, duplicate, or delete action was activated.
