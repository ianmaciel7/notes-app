import * as React from "react";
import { cn } from "@/lib/utils";

export interface {{pascalCase name}}Props extends React.HTMLAttributes<HTMLDivElement> {}

export function {{pascalCase name}}({ className, children, ...props }: {{pascalCase name}}Props) {
  return (
    <div className={cn("rounded-lg border p-4", className)} {...props}>
      {children || "{{titleCase name}} Component"}
    </div>
  );
}
