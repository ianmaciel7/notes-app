# Capacities Component Behavior Map

Reference URL: https://app.capacities.io/eb0a4d1e-0567-4348-8ecf-587c417725f4/a961988a-5562-45bf-86d4-2b2375b17544

Purpose: maintain a working mental map between Capacities components and local KnowledgeOS components. Update this file whenever a Capacities-like UI component is compared, implemented, or fixed.

## Required comparison fields

- Capacities surface/component.
- Local component/file.
- Graphify context query used, when the change is architectural or cross-component.
- Visual geometry: width, height, radius, padding, gaps, density.
- Color tokens: background, border, primary text, secondary text, subtle text, hover/active states.
- Icon system: SVG shape, badge size, inner icon size, stroke/fill behavior, color tone.
- Interaction behavior: click, hover, keyboard, focus, close/open rules, scroll behavior.
- Data source: real Dexie/Firebase/state source, never mock unless explicitly documented as fixture/test data.
- Known gaps and decision notes.

## Current map

| Capacities component | Local component/file | Behavior and visual target | Current notes |
| --- | --- | --- | --- |
| Workspace locale fallback | `src/i18n/request.ts` | The logged-in Capacities reference is in Brazilian Portuguese; menu labels should use the same language when the browser has not selected another supported locale. | Default is `pt-BR`; an existing valid `NEXT_LOCALE` cookie still wins. |
| Command/Search palette | `src/components/app-sidebar-primary-actions-command-dialog.tsx` | Modal command surface, centered desktop card, white front surface, `12px` radius, `672px` desktop width, compact header, recent items first, command actions after recent results, footer shortcuts fixed at bottom. Pointer selection keeps palette open, keyboard Enter closes. Added `Abrir tarefas` action with `Ctrl+Alt+T` shortcut and dedicated `primary-action:tasks` panel entry; removed duplicated calendar action so only “Abrir painel de hoje” remains for `Ctrl+Alt+H`; keyboard global hotkey mirrors this in `space-controller` and action copy remains explicit (`Abrir painel de hoje`) for clarity. | Recent data comes from real workspace entities. Open-in-new-tab visual pill is hidden to match current reference. |
| Command palette row | `src/components/app-sidebar-primary-actions-command-dialog.tsx` + `src/components/object-icons.tsx` | Row uses compact Capacities density: left object badge, title, optional right label chip or shortcut/cursor affordance. Menu badge target is `22px` outer area with `14px` inner SVG. | Avoid Tailwind `w-sm` / `h-sm` for element tokens; use explicit `w-(--el-w-sm)` / `h-(--el-h-sm)` when matching Capacities element sizes. |
| Object icon badge | `src/components/object-icons.tsx` | Tone-specific badge with rounded `0.475em` corners, `0.5px` border, light colored background, tone-colored SVG. | Icon tone classes consume `--type-label-*` tokens from `src/app/globals.css`; do not place literal color values in component classes. Icons must be checked by SVG shape, not only by label or color. Some object types may still need exact Capacities icon path parity. 2026-09-09 aligned the Study goal UI definition key with the persisted `study-goal` icon name so built-in study goals present without a runtime crash. 2026-09-09 replaced the Flashcard glyph with a simplified two-card icon inspired by Tabler-style card glyphs, keeping it cleaner at sidebar size. 2026-09-09 updated Media to the Capacities split-panel media glyph supplied from the live reference. 2026-09-09 centralized built-in visual identity resolution in `getObjectTypeIconAppearance` / `ObjectTypeIconBadge`; `study_goal` now resolves to the `study-goal` graduation-cap glyph and lime tone by canonical id even if old persisted workspace metadata still carries another `iconName` or tone. |
| Sidebar object type rows | `src/components/app-sidebar-primary-actions.tsx`, `src/components/app-sidebar-overview.tsx`, `src/components/app-sidebar-overview-parity.tsx`, and object icon definitions | Persistent leading icon, compact `29px`-`32px` rows, hover-only secondary controls, real object counts. Manual ordering uses row-native drag without a visible grip: press the row, start native drag, reorder on row hover/dragover, and persist order through the active space setting. Object types can be dropped into Fixados, including before a specific pinned row, and pinned items can be dropped into object-type collections. | Secondary action visibility must remain hover-reveal, not always visible. DnD rows and targets expose `data-dnd-*`, `data-pinned-id`, `data-object-type-id`, and `data-collection-id` so browser parity tests can inspect the same contract the user interacts with. |
| Popover/menu inputs | `src/components/ui/input.tsx`, `src/components/app-sidebar-overview.tsx`, `src/components/ui/compact-menu.tsx` | Search/add inputs inside compact popovers must keep typed text readable against muted menu fields. Text color is explicit foreground, placeholder color uses `--app-text-subtle` at full opacity, and selected text uses the primary selection token pair. | 2026-09-09 fixed the pinned-content picker input so it no longer inherits a washed-out color from its menu container. |
| Sidebar object context popup | `src/components/app-sidebar-overview.tsx`, `src/components/ui/compact-menu.tsx`, `src/components/ui/shared-styles.ts` | Object, collection, pinned item, object-type, and sidebar section popups follow the live Capacities `#popup-container > div` capture: `preview-card-core`, `border border-[var(--app-border-front)]` for the captured `0.8px` computed outer stroke, `bg-front`, `text-subtle`, `text-left text-xs font-sans font-normal`, `shadow-[var(--app-shadow-sidebar-popover)]` with exact value `0 3px 5px #00000003, 0 5px 10px #00000005, 0 10px 14px #00000003`, visible `269px` width via `box-content w-[255px]` plus `p-1.5` and the outer border, a `12px` surface radius, `h-base` rows with `text-sm leading-5` (`14px` / `20px` computed), ghost icon frames with `12px` inner icons, `bg-el` hover/highlight/open-submenu state, and `0.5px` `border-[var(--app-border-front)]` separators. Object-type menus expose `Open`, `Create {type}`, `New from Template`, `New Query`, `New Collection`, `Pin to Sidebar`, and type settings instead of a single settings row. `Open` and `Copy` are real nested submenus on object/collection/pinned object menus. Destructive object actions keep the text primary and color only the trash icon red. | 2026-09-09 live comparison found that local `border-front` resolved to the white front-background token, while Capacities computes a gray `0.8px` `border-front` stroke. The local context menu now uses explicit `--app-border-front` for both its `12px` shell and separators, and matches the captured `14px`/`20px` menu-row typography while retaining the `269px` outer width, shadow, action order, icon paths, and real nested submenus. 2026-09-10 corrected the destructive row so it no longer turns the full label red; the row text stays primary while only the trash icon carries `--destructive-menu-action`, including the rendered nested SVG inside the compact icon frame and its hover/highlight states. |
| Sidebar drag insertion preview | `src/components/app-sidebar-overview.tsx`, `src/components/app-sidebar-overview-parity.tsx` | Capacities-style drag previews stay attached to the row being moved and the destination section shows an in-list preview or insertion line instead of a detached generic card. Dropping an object type on the pinned section appends only when no pinned row is targeted; dropping on a pinned row inserts before that row. | Prevents the previously observed failure where object types always became the last pinned item or showed the preview stuck at the first object-type row. |
| Context-menu action dispatch | `src/components/app-sidebar-overview.tsx`, `src/components/app-sidebar-primary-actions.tsx`, `src/components/workspace-main-content.tsx` | Menu selection must perform a real local action or clearly open a named `PendingImplementation`; it must never close with no visible result. Object-type `Open` selects the type list, `Create {type}` uses the existing entity creation path, and object-type template/query/collection/pin actions route to named pending surfaces until implemented. `Copy as Markdown` and `Copy object reference` carry distinct payloads. | `Open` and `Unpin` remain local state/data actions. Collection duplicate/delete retain their existing data behavior. The unsupported actions use `pending:<action>` workspace state and render a named pending surface with a return control. |
| New menu create fallback | `src/components/app-sidebar-primary-actions.tsx`, `src/components/space-controller.tsx` | When the New menu search has no matching type/object, the fallback row can show `Criar 'typed value'` plus an object-type badge, but the created object title must be the exact typed value. The object-type badge communicates the type only; it must not become part of the object title. | 2026-09-09 fixed create-from-query so `valorlimpo123` creates an object/tab titled `valorlimpo123`, not `Untitled valorlimpo123`. |
| Action-side workbench panels | `src/components/workspace-main-content.tsx` | Search, Calendar, Explore, and Tasks action views share a compact header (category/title + return control), per-panel list/empty states, and shared return behavior via `primary-action:*` state markers. Search mantém uma linha ativa com setas `↑/↓`, abre o item ativo por `Enter` (input e lista de resultados), e fecha com `Esc` quando permitido. Explore e Tasks também suportam navegação por teclado (`↑/↓ + Enter`) com destaque da entrada ativa e abertura do item destacado, além de retorno por contexto e foco inicial ajustado. | Return target tracks previous tab, preserving context after action-close interactions. |
| Pending implementation surfaces | `src/components/pending-implementation.tsx`, `src/components/ui/empty.tsx`, `src/components/workspace-main-content.tsx`, `src/components/workspace-side-panel-renderer.tsx`, `src/app/page.tsx` | Old partial placeholders for the Study goal dashboard, Flashcards review panel, and right inspector were replaced/removed in favor of a shared `PendingImplementation` component composed from the design-system `Empty` primitives. The main and side pending surfaces resolve their displayed name from the active tab label (`mainTabs/mainValue` and `sideTabs/sideValue`) so panel body content stays synchronized with the tab above it. Workspace pending routes use `variant="workspace"`: the panel shell remains the outer surface, while `PendingImplementation` itself owns the compact dashed pending card so main and side panels have the same appearance. | The default main panel now shows the active main tab name as pending, and the right side panel shows the active side tab name as pending until each final component is rebuilt. 2026-09-09 normalized workspace pending UI so renderers no longer add side-specific wrappers such as `bg-card p-4`; each unfinished workspace route renders one shared `PendingImplementation` with the same pending-card treatment. |
| Object renderer | `src/components/workspace-object-renderer.tsx`, `src/components/workspace-object-data-view.tsx`, `src/components/workspace-main-content.tsx`, `src/lib/space-object-types.ts`, `src/lib/spaces/space-repository.ts` | Main panel rendering is routed by surface type instead of one generic default panel: object-type root/list tabs render through `WorkspaceObjectTypeListView`, individual object tabs render through `WorkspaceObjectRenderer`, and unsupported actions keep their own action pending panels. `WorkspaceObjectTypeListView` owns the Capacities-style header, search, view state, menus, and real workspace-entity filtering; `WorkspaceObjectDataView` owns the reusable card/list projection. The data view has a compact header, split `Novo` action, `Visão geral`/`Tudo`, persistent per-space/per-type view preferences, fixed-height block previews, real tags, fixed-width gallery cards, and item opening. `Visão geral` is the default object-type mode and renders a sectioned overview: `Recentemente aberto` cards, `Coleções`, and `Queries`. `Tudo` renders the direct object grid/list and owns the inline `Novo Objeto` affordance. Individual unfinished object pages still return only a named `PendingImplementation`. Weblink objects render a tailored URL view following Capacities' Weblink model of a saved URL object with title, description, URL, and notes/reader space. | Weblink metadata extraction is local-only for now: pasted URLs are stored in `properties.url`; title/description enrichment remains future work. 2026-09-09 split the old `WorkspaceDefaultPanel` behavior into explicit list/object/fallback routing. 2026-09-09 selectively reused menu composition from `old`, fixed card geometry from `old-2`, header density references from `old-3`, and the data-view presentation contract from `old-4`; no legacy controller, entity model, or workspace shell is reused. Live comparison aligned the header and card starts with the reference: `pt-4 pb-1.5` header rhythm, `mt-4` toolbar, a two-column gallery, fixed `192px` preview, and `16px` post-grid new-object spacing. The latest live reference capture measures its `507.3125px` grid as two `247.656px x 308px` cards with `12px` inter-card gaps, `12px` corners, `4px` internal gaps, and the low-elevation shadow. 2026-09-10 live comparison of `Visão geral` vs `Tudo` found that both preserve card presentation by default; the meaningful behavior difference is overview sections versus all-objects direct list, with `Novo Objeto` visible only in `Tudo`. The 28px toolbar controls now separately implement count, filter, sort, tag grouping, and layout selection. Cards use the exact `12px` corner rather than the app-wide `xl` radius; type chips consume the same `--type-label-*` tone tokens as their icon badges. Icon-only controls expose names and existing `/ui` tooltips; actions persist per space/type and cover search, header collapse, menu toggles, filtering, ordering, grouping, list/gallery mode, and the split New menu. Filtered empty states deliberately offer `Limpar filtros` instead of a misleading create action; tag grouping includes every tag on multi-tag entities. |
| Workspace URL state | `src/components/space-controller.tsx`, `src/app/[...workspace]/page.tsx`, `src/components/app-sidebar-primary-actions.tsx`, `src/components/app-header-tabs.tsx` | Capacities-like workspace navigation should be visible in the browser address. Local state remains the source of truth, but the active main tab is mirrored to `/<spaceGuid>/<objectGuid>` using clean GUID-like route segments instead of internal ids such as `personal` or `entity-*`. The side tab is deliberately stored in local browser workspace state instead of the URL, matching the observed Capacities behavior where right-panel item changes do not alter the address. Route restoration happens after mount so the server and first client render share the same default tab tree, avoiding hydration drift before the deep-link state is applied. | A catch-all app route keeps deep workspace URLs rendering the shell on refresh. Full reconstruction of closed tab metadata is still limited; this currently preserves and restores active ids within the live shell session. |
| Main workspace tabs | `src/components/app-header-tabs.tsx`, `src/components/space-controller.tsx`, `src/components/app-sidebar-primary-actions.tsx` | Item tabs and object-type list tabs are distinct. A specific object tab uses the object title as its label and normal text weight when inactive. An object-type list tab uses the plural type label, keeps the active front-surface tab treatment, shows the colored object badge, and uses a stronger active label. | 2026-09-09 reference comparison: `rebase` is an item tab, while `Páginas` is a list tab. Local tabs carry `kind` metadata (`object`, `object-list`, `collection`, `action`) so restored tabs, sidebar-opened tabs, and created object tabs keep the correct visual behavior. |
| Main workspace tab persistence | `src/components/space-controller.tsx`, `src/components/app-header-tabs.tsx` | Open/closed main-tab membership persists across refresh independently from the URL. The browser address mirrors the active tab, while local workspace state stores stable tab ids and active id. On refresh, saved tab membership is restored after workspace data is ready and stale route segments do not recreate tabs the user closed. The header keeps neutral tab space while route/tab restoration is pending, avoiding a temporary fallback `Pages` tab flash. | 2026-09-09 fixed F5 behavior after closing a main tab: closed tabs remain closed instead of being resurrected from the current route. Stored tabs are reconstructed from real object types/entities so icons and labels stay current. |
| Explore side panel | `src/components/workspace-side-panel-content.tsx`, `src/components/workspace-side-panel-renderer.tsx`, `src/components/workspace-main-content.tsx`, `src/components/app-side-panel-header.tsx`, `src/components/space-controller.tsx` | `WorkspaceSidePanelContent` reads the real workspace state and resolves the active side tab/context; `WorkspaceSidePanelRenderer` owns the rendered side-panel surface. Side panel content intentionally renders only one `PendingImplementation` for the active side tab. `Explore` itself is pending, and each available special entry renders as its own pending surface when opened. Special item selection follows Capacities tab behavior: if the default `Explore` tab is the only side tab, selecting an available special item replaces that tab; if multiple side tabs exist, the item is selected or appended like the existing workspace tab model. Closing the final side tab resets the stored side state back to `Explore` before the panel is hidden, so reopening the right panel returns to the Capacities-style Explore base tab instead of the last special item. Entry availability is context-sensitive: object-type list views expose only AI chat and Search; specific object pages expose Graph view, Backlinks, Objects inside, Related content, AI chat, and Search; collection views expose Graph view, AI chat, and Search. | Graph view remains scoped to the active object or collection per Capacities evidence. Real Explore overview/actions and backlink/object-inside projections are deferred. Ladle exposes `Workspace side panel content` separately from header/shell stories. |
| Add object type modal | `src/components/app-sidebar-object-type-studio.tsx`, `src/components/object-icons.tsx`, `src/lib/space-object-types.ts` | Modal matches Capacities card grid: desktop dialog width `1128px`, tokenized card surface `--app-bg-el-subtle`, border `--app-border-el`, hover `--app-bg-el-subtle-hover` / `--app-border-el-hover`, selected surface `--app-bg-el-subtle-active`, label text `--app-text-primary`, `32px` icon badge, and `text-sm font-semibold` labels. Intro guidance is a compact green callout, `max-w-[672px]`, rounded `8px`, on `--app-bg-front` content surface. Details panel opens on object-type card selection; clicking non-interactive backing content closes the panel while card, link, and form-control clicks keep their own behavior. | Current screenshot tone map is enforced by shared `getCapacitiesObjectTypeTone`: colored `Livro`, `Área`, `Definição`, `Flashcard`, `Ideia`, `Projeto`, `Mídia`, `Viagem`, `Nota atômica`, `Página`, `Weblink`, `Tweet`, `Chat de IA`, `Meta de estudo`, `Tabela`, `Query`; gray `Pessoa`, `Reunião`, `Citação`, `Lugar`, `Organização`, `Crie o seu próprio`, `Etiqueta`, `Imagem`, `PDF`, `Áudio`, `Arquivo`, `Tarefa`. Atomic note uses `CardsThreeIcon`; Flashcard uses `StackPlusIcon` with fuchsia so it does not read as Atomic note or Definition; Study goal uses `GraduationCapIcon` with lime so it does not read as Project, Query, Place, Area, or Table. 2026-09-09 screenshot comparison fixed missing `--app-block-bg-green` / `--app-block-text-green` tokens, replaced the full-width/back-tinted body look with a front-surface modal body, reduced the desktop shell from `1152px` to `1128px`, restored the app-specific built-ins `Flashcard` and `Study goal` to the Basic types picker, and kept card, button, callout, and object-type control colors in `src/app/globals.css` tokens instead of component-level OKLCH fallbacks so dark mode stays aligned. |
| Sidebar section geometry and DnD contract | `src/components/app-sidebar-overview.tsx`, `src/components/app-sidebar-overview-parity.tsx` | Live Capacities measurement at `1280x912`: sidebar `288px`; sticky section header `32px`; row `29px`; row content width `262px` inside a `278px` section body; inner row radius `8px`; default row transition `200ms cubic-bezier(0,0,0.2,1)`; section reveal motion `150ms ease-out`. Draggable rows expose `data-dnd-type="draggable"` and `data-dnd-id`; section and collection targets expose `data-dnd-type="droppable"`. | 2026-09-10 parity pass changed the local section header from `24px` to `32px`, constrained the content body to the same `10px` inset, and added the DnD attributes. Local dark-mode tokens remain intentionally different from the light reference capture. |
| Sidebar drag insertion | `src/components/app-sidebar-overview-parity.tsx` + `src/components/app-sidebar-overview.tsx` | Capacities uses a draggable row plus a droppable list/section model; the target row is the insertion anchor, not an append-only destination. The local flow keeps native `DataTransfer` plus pointer fallback, records the hovered row, renders a `29px` contextual preview, and persists the resulting order through the existing Dexie-backed pinned projection. | 2026-09-10 audit found a row-level `onDrop` that still appended object types with `setPinned([...current, item])`; it now calls `pinObjectType(id, targetId)` so dropping on a row inserts before that row. Learned behavior: moving object types into Fixados should preserve the exact hovered insertion point and keep the same preview row treatment used inside the Tipos de objeto section. |
| Architecture/dependency graph | `graphify-out/graph.json`, `graphify-out/graph.html`, `graphify-out/GRAPH_REPORT.md` | Graphify is the source for local architecture navigation and impact mapping. Use `graphify query`, `graphify explain`, `graphify path`, and `graphify affected` to understand component relationships before cross-cutting parity edits. | Last updated after the 2026-09-10 object-type overview parity pass: `11562` nodes, `22235` edges, `747` communities. Hooks are installed and merge driver is registered for `graphify-out/graph.json`. |

## Working rule

2026-09-10 pasted Capacities HTML evidence for object-type tabs: active `Visão geral` uses the front-border/front-surface tab treatment with `px-3.5`, inactive `Tudo` keeps subtle text plus hover surface, and empty `Coleções`/`Queries` sections render as centered simple empty blocks rather than framed cards. Local `WorkspaceObjectTypeListView` should keep those details unless newer reference evidence supersedes them.

Before changing any Capacities-parity component, update or consult this map. If a screenshot or live reference disagrees with implementation, record the measured difference and fix the local component using explicit tokens/classes instead of ambiguous Tailwind names.

## Graphify workflow

- Ask targeted questions with `graphify query "<question>"`.
- Inspect a module or component with `graphify explain "<node>"`.
- Find relationships with `graphify path "<source>" "<target>"`.
- Check impact with `graphify affected "<node>"`.
- Refresh the graph after meaningful code changes with `graphify update .`.
- Check integration with `graphify hook status`.
- Do not use Graphify output as a replacement for source review when editing a specific file; use it to choose where to inspect and what dependencies may be affected.
